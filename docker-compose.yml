version: '3.8'

services:
  web:
    image: php:8.2-apache
    volumes:
      - ./src:/var/www/html
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
  image: mysql:8.0
  environment:
    MYSQL_ROOT_PASSWORD: example
    MYSQL_DATABASE: testdb
  ports:
    - "3307:3306"

